{
  "name": "riftbound-card-tracker",
  "version": "0.1.0",
  "description": "Telegram bot for Riftbound card lookup and pricing",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "lint": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "telegraf": "^4.16.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
